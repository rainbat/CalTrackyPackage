import XCTest
@testable import CalTrackyPackage

final class CalTrackyPackageTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(CalTrackyPackage().text, "Hello, World!")
    }
}
